#include<stdio.h>
int main ()
{
    float x,y;
    printf("enter x :");
    scanf("%f",&x);

    printf("enter y :");
    scanf("%f",&y);

    printf("sum is = %f \n", x + y);
     printf("diff is = %f \n", x - y);
      printf("multy is = %f \n", x * y);
       printf("divi is = %f \n", x / y);
       printf("student id is : 24TCEZWT");
    return 0;
}
